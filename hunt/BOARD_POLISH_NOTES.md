# Hunt Board proportion patch

- Keeps the board controls inside the viewport on desktop.
- Reduces the map and contract-detail footprint.
- Makes the contract list horizontally swipeable on phones.
- Stacks map, contract details, and navigation into a natural mobile page.
- Uses the display name White Tree while preserving the stable `black-veil` save ID.
